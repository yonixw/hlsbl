{
  "name": "hlsbl",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "parse1": "deno run --allow-read parse.ts examples/1-helloworld.hlsbl",
    "reload": "deno cache --lock=lock.json --lock-write **/*.ts"
  }
}
